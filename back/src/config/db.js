// require('dotenv').config()

const dbOptions = {
    // host: process.env.HOST,
    host: 'localhost',
    user: 'lucas',
    password: 'rfn2K22$',
    port: 3306,
    database: 'react_db'
};

module.exports = dbOptions;